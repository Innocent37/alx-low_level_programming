#include "main.h"
/**
 *positive_or_nagative - checks if the number nagative or positive
 *
 *@i: is the number checked
 *Return: always 0
 */
void positive_or_negative(int i);
{
	if (i < 0)
		printf("%d is nagativ\ne", i);
	else
		printf("%d is positive\n", i);
}
